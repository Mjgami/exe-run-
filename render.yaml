services:
  - type: web
    name: wine-novnc-app
    env: docker
    plan: free
    disk:
      name: appdata
      mountPath: /root/app
      sizeGB: 1
    envVars:
      - key: VNC_PASSWORD
        value: "StrongPassword123"  # Change this
    autoDeploy: true

