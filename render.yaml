services:
  - type: web
    name: exe-run
    env: docker
    dockerfilePath: ./Dockerfile
    plan: starter   # free will sleep; use 'starter' or above for 24/7
    autoDeploy: true
    envVars:
      - key: VNC_PASSWORD
        value: "CHANGE_ME_TO_A_STRONG_PASSWORD"
