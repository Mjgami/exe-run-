services:
  - type: web
    name: exe-vnc-server
    env: docker
    dockerfilePath: ./Dockerfile
    plan: starter   # use paid starter/pro for 24/7; free will sleep
    autoDeploy: true
    envVars:
      - key: VNC_PASSWORD
        value: "CHANGE_THIS_TO_A_STRONG_PASSWORD"
